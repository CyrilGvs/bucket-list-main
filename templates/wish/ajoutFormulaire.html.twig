{% extends 'base.html.twig' %}

{% block title %}{{ parent() }} | ADD IDEA{% endblock %}

{% block CSS %}
    <meta charset="UTF-8">
    {{ parent() }}
    <link rel="stylesheet" href="{{ asset('css/ajoutFormulaireCSS.css') }}">
    <link rel="icon" href="{{ asset('img/favicon.png') }}">
{% endblock %}

{% block body %}

    <div class="form">
{#        {{ form_start(monFormulaire) }}#}
{#        {{ form_widget(monFormulaire) }}#}
{#            <button>Envoyer</button>#}
{#        {{ form_end(monFormulaire) }}#}
        {{ form_start(monFormulaire) }}
        {{ form_row(monFormulaire.title) }}
        {{ form_row(monFormulaire.description) }}
        {{ form_row(monFormulaire.author) }}
        {{ form_row(monFormulaire.categories) }}
        <button>Envoyer</button>
        {{ form_end(monFormulaire) }}
    </div>

{% endblock %}